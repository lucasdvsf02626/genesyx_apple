# Worklog — 2026-07-29

## Client structural changes (pH spine, Track order, hydration units, supplements, nutrition)

Branch `main`. Build number deliberately **unchanged** (1.1.1 / 16) — CI pbxproj fix in flight.
Incremental commit + test per phase. All health copy uses the existing citation infrastructure.

### Phase 0 / 1 (prior verification, carried forward)
Client's "Urine pH / 4.5–9.0 / 6.0–7.5" findings are from a **pre-migration build**; current HEAD is
post-migration (vaginal, 3.5–7.0, two-band). Zero reachable urine pH views/strings. PASS.

### Phase 1 — Track order  ✅ `c96f253`
Cycle → Vaginal pH → Nutrition → Symptoms → Sleep → Hydration.

### Phase 2 — pH product spine  ✅ `3da373f`
Four cited sections on the pH detail view (Track + Insights, `variant .full`): Why pH matters
(+ SourcesFooter: vaginal-ph NHS, statpearls-vaginitis), What this result means (per band), What to
do next (per band), Genesyx supplements (navigational → Nutrition tab; no causal claim). Copy in
`PhCopy`; verbatim + banned-phrase guards extended.

### Phase 3 — Hydration units  ✅ `1bb379c`
`HydrationUnit`/`HydrationFormat` in core (glasses primary, ml secondary; 1 glass = 250 ml; display
only, storage stays ml). Profile toggle (`@AppStorage "hydration_unit"`, default glasses). Readouts
on Home / Nutrition / Track row / detail sheet honour the unit; detail quick-add is +1/+2 glasses in
glasses mode. Fixed the mixed-unit bug on the Nutrition card.

### Phase 4 — Manual supplements  ✅ `f570bd4`  (⚠️ DB flagged)
`CustomSupplement` core model + `SupplementPlanSheet` with Genesyx essentials first-class and an
"Add your own supplement" form (name, dose, time) + delete. **Persists LOCALLY only** (`@AppStorage`
JSON). **Supabase table NOT added** — remote persistence must match the Android schema, which isn't
confirmed here. FLAGGED, not applied (per the standing rule). Needs the Android table name + columns
before remote wiring.

### Phase 5 — Nutrition re-prioritisation  ✅ `369a902`
Phase focus foods lead; hydration + general articles drop below. Honest "coming soon" empty state for
meal suggestions + food preferences (no fabricated content).

### Missing content (not built — no fake data added)
- Meal suggestions (per phase) — empty state only.
- Food preferences / dietary filters — empty state only.
- Custom-supplement remote sync — blocked on Android schema.

### Verification gate
| Item | Result |
|---|---|
| Zero reachable urine pH views/strings | ✅ PASS |
| pH view answers all four questions w/ citations | ✅ PASS |
| Track order Cycle → pH → Nutrition → rest | ✅ PASS |
| Manual supplement entry works | ✅ PASS (local); remote FLAGGED (Android schema) |
| Hydration defaults to glasses, data untouched | ✅ PASS |
| Calendar legible, no "..." | ✅ PASS (already true) |
| Test suite green | ✅ core 127 · app unit 138 · UI 23 (1 skip) · 0 failures |
| Build number unchanged | ✅ 1.1.1 (16) |

**State: WORKING** (full suite green; build number unchanged).

### Next
- Paste the Android `custom_supplements` (or equivalent) schema → wire remote sync + propose (not
  apply) the matching Supabase migration.
- Author meal-suggestion / food-preference content when available.
