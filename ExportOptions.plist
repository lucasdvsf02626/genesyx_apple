<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store</string>

    <key>teamID</key>
    <string>M5L3MM75SG</string>

    <!-- MANUAL signing: we already have the "Genesyx App Store" distribution
         profile + matching Apple Distribution cert installed locally, so no
         API key is needed. Manual signing at EXPORT time re-signs ONLY the app
         bundle (com.genesyx.app); SPM frameworks are signed with the same
         identity but never receive a provisioning profile. -->
    <key>signingStyle</key>
    <string>manual</string>

    <key>signingCertificate</key>
    <string>Apple Distribution</string>

    <key>provisioningProfiles</key>
    <dict>
        <key>com.genesyx.app</key>
        <string>Genesyx App Store</string>
    </dict>

    <key>uploadSymbols</key>
    <true/>
    <key>stripSwiftSymbols</key>
    <true/>

    <!-- Keep build 8 exactly as set in project.yml; don't let Xcode bump it. -->
    <key>manageAppVersionAndBuildNumber</key>
    <false/>
</dict>
</plist>
